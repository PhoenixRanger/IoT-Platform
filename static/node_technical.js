const root=document.getElementById("technicalRoot"),nodeId=root.dataset.nodeId;
function display(value,suffix=""){return value===null||value===undefined||value===""?"Unknown":`${value}${suffix}`;}
function row(label,value){const wrapper=document.createElement("div"),term=document.createElement("dt"),detail=document.createElement("dd");wrapper.className="detail-row";term.textContent=label;detail.textContent=value;wrapper.append(term,detail);return wrapper;}
function capabilityList(items,empty,{counts=false,checks=false}={}){const wrapper=document.createElement("div");if(!items.length){wrapper.className="capability-empty";wrapper.textContent=empty;return wrapper;}const list=document.createElement("ul");list.className="capability-list";items.forEach(item=>{const entry=document.createElement("li");entry.textContent=`${checks?"✓ ":""}${item.display_name}${counts&&item.count>1?`  ${item.count}×`:""}`;list.append(entry);});wrapper.append(list);return wrapper;}
function renderCapabilities(node){const target=document.getElementById("capabilityDetails");target.replaceChildren();[["Expected",node.capabilities.expected,"None",{counts:true}],["Reported",node.capabilities.reported,node.capabilities.reported_at?"None":"Not reported",{checks:true}],["Missing",node.capabilities.missing,"None",{}],["Additional / Unexpected",node.capabilities.unexpected,"None",{}]].forEach(([heading,items,empty,options])=>{const title=document.createElement("h3");title.textContent=heading;target.append(title,capabilityList(items,empty,options));});target.append(row("Capability State",display(node.capabilities.state)),row("Reported at",display(node.capabilities.reported_at)));}
async function refresh(){const responses=await Promise.all([fetch(`/api/nodes/${encodeURIComponent(nodeId)}`),fetch(`/api/nodes/${encodeURIComponent(nodeId)}/hardware-allocation`),fetch(`/api/nodes/${encodeURIComponent(nodeId)}/runtime-configuration`)]);if(responses.some(response=>!response.ok))throw new Error();const [node,allocation,runtime]=await Promise.all(responses.map(response=>response.json()));nodeHeading.textContent=`${node.name} — Technical`;nodeTechnicalId.textContent=node.node_id;statusDetails.replaceChildren(row("Status",display(node.status)),row("Overall health",display(node.health)),row("Last seen",display(node.last_seen)),row("RSSI",display(node.rssi," dBm")),row("Uptime seconds",display(node.uptime_seconds)));hardwareDetails.replaceChildren(row("Node type",display(node.node_type)),row("Hardware Platform",display(node.hardware_platform?.display_name)),row("Manufacturer",display(node.hardware_platform?.manufacturer)),row("Model",display(node.hardware_platform?.model)),row("MCU",display(node.hardware_platform?.mcu)),row("Revision",display(node.hardware_platform?.revision)));firmwareDetails.replaceChildren(row("Name",display(node.firmware_name)),row("Version",display(node.firmware_version)),row("OTA hostname",display(node.ota_hostname)));renderCapabilities(node);configurationSummary.replaceChildren(row("Physical Configuration",allocation.mapping_state),row("Runtime Configuration","Valid"),row("Current Revision",runtime.runtime_configuration_revision));}
refresh().catch(()=>nodeError.hidden=false);
